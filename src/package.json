{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-js": "webpack --mode development --watch --entry ./js/script.js -o ../dist",
    "build-js": "webpack --mode production --entry ./js/script.js -o ../dist",
    "dev-css": "npx tailwindcss -i ./input.css -o ../dist/style.css --watch",
    "build-css": "npx tailwindcss -o ../dist/style.css --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.5",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
